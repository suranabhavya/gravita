# Database Configuration
# Option 1: Use connection string (recommended for Neon)
# DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# Option 2: Use individual components
DB_HOST=ep-flat-cherry-a4ztfgoy-pooler.us-east-1.aws.neon.tech
DB_PORT=5432
DB_USER=neondb_owner
DB_PASSWORD=your_password_here
DB_NAME=neondb
DB_SSL_MODE=require

# Note: For Neon PostgreSQL, SSL is required. Set DB_SSL_MODE=require

# Application Configuration
NODE_ENV=development
PORT=3000

# JWT Configuration (for future use)
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d

# Resend Email Configuration
# Set ENABLE_EMAIL_SENDING=true to send real emails (requires verified domain)
# Set ENABLE_EMAIL_SENDING=false to log OTPs to console (development mode)
ENABLE_EMAIL_SENDING=false
RESEND_API_KEY=your_resend_api_key_here
RESEND_FROM_EMAIL=onboarding@resend.dev

